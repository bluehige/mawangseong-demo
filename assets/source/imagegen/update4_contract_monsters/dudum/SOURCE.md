# 기본 계약 전투 두둠 생성 기록

- Generation model: GPT internal image generation
- Generated date: 2026-08-04
- Target version: v1.2.2
- Source image path: `assets/source/imagegen/update4_contract_monsters/dudum/dudum_combat_sheet_chroma_2026-08-04.png`
- Runtime image path: `assets/sprites/monsters/update4/monster_dudum_sheet.png`

## 목적

기존 `war_drummer`가 `goblin`·`mimic_porter`와 고블린 idle 그림을 공유하던 문제를 해결하기 위한 두둠 전용 기본 계약 전투 자산이다. 작은 해골 북잡이의 전투 역할이 한눈에 보이도록 북·북채·깃털을 유지하고, 돌콩·모리와 같은 4×4 전투 시트 계약을 따른다.

## 최종 생성 사양

- 4열×4행, 총 16셀, 셀당 캐릭터 1개
- 행 순서: 1행 `idle_down` 2프레임 + `down` 2프레임, 2행 `move_down` 4프레임, 3행 `attack_down` 4프레임, 4행 `skill_down` 4프레임
- 작은 해골 전투 북잡이: 숯빛 뼈, 낡은 버건디 천, 녹슨 갈색 원형 북, 절제된 황동 테두리와 북채, 검은 행진 깃털
- `down` 두 프레임은 북을 옆에 둔 명확한 옆으로 쓰러진 자세이며, `move`·`attack`·`skill` 행과 섞이지 않도록 재생성에서 행 계약을 명시했다.
- 동일한 쿼터뷰 전투 카메라, 셀 안의 넉넉한 여백, 최종 런타임의 공통 바닥 기준선 `y=183`
- 원본 배경: 완전히 평평한 크로마 그린 `#00ff00`; 글자·라벨·격자선·워터마크·추가 캐릭터 없음

## 프롬프트 및 반복 기록

GPT 내부 이미지 생성 모델을 기본 생성 경로로 사용했다. 첫 생성은 캐릭터 정체성·색상·4×4 배치는 통과했지만 1행의 `down` 두 칸이 서 있는 공격 자세로 나와 폐기했다. 두 번째 생성은 1행 3·4칸을 옆으로 쓰러진 자세, 2행을 걷기, 3행을 북 공격, 4행을 황동 리듬 지원으로 고정해 최종 원본으로 채택했다. 돌콩·모리 시트는 색감과 카메라를 맞추기 위한 스타일 참고로만 사용했다.

## 후처리

- `remove_chroma_key.py`의 자동 경계 키, soft matte, despill로 크로마 그린을 제거했다.
- 1254×1254 원본의 셀 경계 `[0, 314, 627, 941, 1254]`를 사용하고, 8방향 연결 성분을 다수 셀 기준으로 배정해 셀 경계를 넘은 작은 장식·이펙트를 원래 프레임으로 복구했다.
- 공통 패딩 원본 창과 premultiplied LANCZOS 축소로 16개 투명 192×192 셀을 만들고 768×768 RGBA 시트로 조립했다.
- 생성 프레임별 높이 차이로 캐릭터가 뜨지 않도록 각 셀의 보이는 바닥을 `y=183`으로 맞췄다. 런타임 검수에서 모든 프레임의 바닥 spread는 `0px`이다.
- 축소 후 남은 녹색 우세 가장자리 픽셀은 투명 처리하고, 투명 모서리·셀 외곽·중복 프레임 해시·크로마 잔류를 직접 검사했다.
- 초상화·VFX·오디오·제품 데이터 연결은 이 패킷에서 생성하지 않았다. 데이터 연결은 다음 별도 패킷으로 둔다.
