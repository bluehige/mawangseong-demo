# Shieldbearer Enemy Sprite Source

Date: 2026-07-09

Generated by `tools/generate_shieldbearer_enemy_assets.py`.

Purpose:

- Enemy id: `shieldbearer`
- Display name: `왕국 방패병`
- Combat role: slow, sturdy Chapter 2 counter unit
- Required animations: `idle_down`, `move_down`, `attack_down`, `skill_down`, `down`

The sprite set is deterministic and can be regenerated locally with:

```powershell
python tools\generate_shieldbearer_enemy_assets.py
godot --headless --path . --import --quit-after 1
```
