# 몬스터 애착·분기 진화·다중 엔딩·다회차 확장 개발 계획

- 작성 기준일: 2026-07-12
- 기준 저장소: `bluehige/mawangseong-demo`
- 로컬 기준 브랜치: `codex/map-custom-2026-07-03`
- 기준 커밋: `0532b46` (`Fix engineer spawn and disable performance`)
- 원 기획 공유 링크: <https://chatgpt.com/share/6a53667e-6104-83ee-896f-4d80f861ed7b>
- 문서 성격: 공유 대화에서 확인한 핵심 설계와 작업 순서를 현재 저장소에 맞춰 복원한 실행 기준서

> 공개 공유 페이지에서 원본 첨부 Markdown의 다운로드 동작이 실패해, 이 파일은 원본 2,679줄 문서의 바이트 단위 복사본이 아니다. 공유 페이지에서 직접 확인한 설계 요약, Phase 순서, 금지 범위, 현재 저장소 코드와 인수인계 문서를 대조해 만든 저장소용 실행본이다.

## 1. 목표

현재 완성된 DAY 1~30 캠페인을 다시 만드는 대신, 다음 다섯 축을 추가한다.

1. 이름 있는 몬스터를 종족이 아닌 개별 동료로 저장한다.
2. 각 핵심 몬스터가 역할과 외형이 다른 두 갈래로 진화한다.
3. 유대, 추억, 스킬, 전투 기록으로 몬스터에 대한 애착을 만든다.
4. 30일 동안의 플레이 기록을 종합해 여러 엔딩을 결정한다.
5. 엔딩 도감과 제한된 계승을 가지고 DAY 1부터 새 회차를 시작한다.

최종 목적은 수치를 무한히 올리는 반복 플레이가 아니다. 다음 회차에 다른 몬스터, 다른 진화, 다른 운영 방식, 다른 엔딩을 선택하고 싶게 만드는 것이 목적이다.

## 2. 바꾸지 않는 계약

- 정규 캠페인의 마지막 날은 DAY 30이다.
- DAY 31~40을 덧붙이지 않는다.
- 관리 → 준비/원정 → 전투 → 결산 → 다음 날의 기본 흐름을 유지한다.
- 기존 4단계 마왕성, 11개 구역, 시설, 지침, 직접 조종을 보존한다.
- 기존 저장 v1은 손실 없이 읽을 수 있어야 한다.
- 전투 불능은 영구 사망이나 유대 감소로 연결하지 않는다.
- 한 회차에서 푸딩·곱·핀을 모두 진화시키지 못하는 현재 승급 수 제한을 유지한다.
- 신규 기능은 `GameRoot.gd`에 계속 직접 쌓지 않고 책임을 분리한다.

## 3. 현재 상태와 확장 지점

현재 저장소에는 이미 다음이 있다.

- DAY 1~3 온보딩과 DAY 4~30 정규 캠페인
- Stage 01~04 마왕성 진화
- 원정, 전술 특화, 승급, 최종 공성전
- 승리 엔딩 `true_demon_castle`
- 패배 후 재도전 `named_rivals_retry`
- 안전 교체, 손상 검증, 임시 파일과 백업 복구를 포함한 저장 v1
- Quick/Full 핵심 통합 검증과 화면 캡처 검사

현재 `monster_roster`는 `slime`, `goblin`, `imp`, `kobold_scout` 같은 종족 ID가 곧 저장 키다. 이 구조에서는 같은 종족의 두 개체, 개별 유대, 개별 추억, 한 마리 계승을 안전하게 표현하기 어렵다.

현재 성장은 DAY 2 전술 특화와 DAY 12/23 승급을 이미 지원한다. 이를 폐기하지 않고 다음 세 층으로 분리한다.

```text
전술 특화 2개 × 형태 진화 2개 × 장착 스킬
```

- 전술 특화: AI와 전투 성향
- 형태 진화: 역할, 외형, 핵심 기술
- 장착 스킬: 같은 진화 안에서의 세부 빌드

## 4. 식별자 계약

전문 용어인 식별자는 데이터를 서로 헷갈리지 않게 구분하는 고유 이름이다.

| 필드 | 예시 | 의미 |
|---|---|---|
| `species_id` | `slime` | 종족과 기본 전투 원형 |
| `instance_id` | `mon_core_pudding` | 저장을 넘어 추적하는 개별 몬스터 |
| `character_id` | `CHR_PUDDING` | 초상과 대사에 쓰는 서사 캐릭터 |
| `runtime_id` | `battle_30_mon_core_pudding_01` | 한 전투 안에서 생성된 실제 유닛 |

기존 4명의 고정 변환은 다음과 같다.

| 기존 키 | `instance_id` | 이름 | `character_id` |
|---|---|---|---|
| `slime` | `mon_core_pudding` | 푸딩 | `CHR_PUDDING` |
| `goblin` | `mon_core_gob` | 곱 | `CHR_GOB` |
| `imp` | `mon_core_pynn` | 핀 | `CHR_PYNN` |
| `kobold_scout` | `mon_core_rolo` | 로로 | `CHR_ROLO` |

## 5. 개별 몬스터 데이터

각 개체는 최소한 다음 자료를 가진다.

```json
{
  "instance_id": "mon_core_pudding",
  "species_id": "slime",
  "character_id": "CHR_PUDDING",
  "display_name": "푸딩",
  "level": 1,
  "exp": 0,
  "bond": 0,
  "bond_rank": 0,
  "specialization_id": "",
  "evolution_id": "",
  "equipped_skill_ids": [],
  "unlocked_memory_ids": [],
  "run_records": {},
  "lifetime_records": {}
}
```

유대는 반복 선물 버튼으로 올리지 않는다. 실제 출전, 역할 수행, 구조, 방어, 원정, 개인 사건, 추억 선택으로 오른다. 벤치에 있었다는 이유로 떨어지지 않는다.

## 6. 분기 진화

### 6.1 1차 핵심 진화

| 몬스터 | 방어/공격 중심 가지 | 운영/지원 중심 가지 |
|---|---|---|
| 푸딩 | 성문 방벽 푸딩 | 구조 연금젤 푸딩 |
| 곱 | 매복대장 곱 | 금고지기 곱 |
| 핀 | 화염 숙련자 핀 | 잿불 주술사 핀 |

로로의 두 갈래는 푸딩 수직 구현과 곱·핀 확장을 검증한 뒤 확정한다. 공유 기획은 로로를 장기 성장 대상으로 포함하지만, 1차 표에서는 푸딩·곱·핀의 경로를 먼저 고정했다.

### 6.2 선택 규칙

- DAY 12부터 첫 진화 1명
- DAY 23부터 누적 2명
- 확정 전 역할, 스킬, AI 변화, 장단점을 모두 보여 준다.
- 현재 회차에서는 진화를 취소하지 않는다.
- 다음 회차에서 다른 경로를 선택할 수 있다.
- 한 경로가 다른 경로의 완전한 상위 호환이 되지 않게 한다.

## 7. 스킬 구조

스킬 효과를 JSON 안의 임의 코드로 실행하지 않는다. 등록된 효과 종류만 읽는 데이터 기반 실행기를 사용한다.

초기 효과 종류는 실제 필요한 최소 범위로 제한한다.

- 단일 피해
- 범위 피해
- 보호막
- 회복
- 이동/도발
- 상태 효과 부여
- 시설 또는 아군 대상 보조

푸딩 한 마리의 두 진화와 신규 기술을 끝까지 연결해 검증하기 전에는 모든 몬스터 스킬을 한꺼번에 만들지 않는다.

## 8. 엔딩 지표

엔딩은 마지막 선택 하나가 아니라 전체 회차 기록의 결론이어야 한다.

수집할 지표:

- 몬스터별 유대와 추억
- 왕좌 HP, 보안 등급, 보물 손실, 시설 무력화
- 악명과 위험 원정
- 전체 지침 사용 비율
- 레온·니아·바티·로로 관계
- 식구·철벽·위압·명예·계략의 다섯 기풍
- DAY 25·28·29의 주요 결정

### 8.1 조건식 DSL

DSL은 복잡한 코드를 직접 넣지 않고 허용된 규칙만 조합하는 작은 조건 문법이다.

허용 구조:

```json
{
  "all": [
    {"metric": "bond.core_average", "op": ">=", "value": 70},
    {"metric": "style.family", "op": ">=", "value": 60}
  ]
}
```

허용 조합:

- `all`: 모두 참
- `any`: 하나 이상 참
- `not`: 조건 반전
- 비교: `==`, `!=`, `>`, `>=`, `<`, `<=`, `contains`

허용된 metric 목록 밖의 이름, 잘못된 자료형, 알 수 없는 연산자는 로딩 단계에서 실패시킨다. `eval` 같은 임의 코드 실행은 금지한다.

### 8.2 첫 구현 엔딩 5개

1. `true_demon_castle` — 균형형 기본 엔딩
2. `monster_family_castle` — 몬스터 식구 엔딩
3. `impregnable_demon_citadel` — 철벽 요새 엔딩
4. `dread_overlord_rises` — 악명·위압 엔딩
5. `demon_hero_rival_pact` — 레온 라이벌 협정 엔딩

후속 범위:

- 니아·골딘 중심 계략 엔딩
- 바티 비밀 엔딩

판정 순서는 하드 조건 → 점수 → 동률 규칙 → 기본 엔딩 순으로 고정한다. 같은 저장 데이터는 항상 같은 엔딩을 내야 한다.

## 9. 다회차와 저장

한 개의 안전한 v2 저장 안에서 영구 자료와 현재 회차 자료를 논리적으로 분리한다.

```json
{
  "version": 2,
  "profile": {},
  "active_run": {}
}
```

### 9.1 유지하는 것

- 엔딩 도감
- 추억 해금
- 본 개인 사건
- 계약서/모집 해금
- 발견한 진화와 기술 정보
- 완료 회차 수
- 선택한 계승 몬스터 한 마리의 제한된 추억

### 9.2 초기화하는 것

- 레벨과 경험치
- 현재 자원
- 진화 형태
- 시설 배치와 강화
- 현재 회차 전투 기록
- 현재 회차 관계 점수

### 9.3 2회차 이후 편의 기능

- 왕국 교리 선택
- 이미 본 대사 건너뛰기
- 추가 전투 배속
- DAY 1~3을 정상 완료 상태로 처리하는 DAY 4 압축 시작

편의 기능은 1회차의 학습 흐름을 건너뛰지 않으며, 압축 시작은 필요한 플래그·보상·관계 초기값을 자동 검증해야 한다.

## 10. 그래픽 리소스 계약

시스템 검증 전에 모든 그래픽을 일괄 생성하지 않는다. 푸딩 두 진화를 임시 이미지로 실제 전투에서 먼저 검증한 뒤 제작한다.

제작 순서:

```text
임시 이미지 전투 검증
→ 디자인 시트
→ 초상
→ 전투 프레임
→ VFX
→ UI 배지/아이콘
```

예상 1차 수량:

- 진화 형태 6개
- 형태당 `idle 2 / move 4 / attack 4 / skill 4 / down 2`
- 총 96개, 192×192 RGBA 전투 프레임
- 진화 초상 18개
- 진화 배지 6개
- 기존·신규 스킬 아이콘 약 18개
- 신규 VFX 6세트
- MVP 엔딩 일러스트 5개
- 엔딩 문양과 도감 썸네일
- 유대·추억·기풍 UI 리소스

모든 생성 원본에는 `SOURCE.md` 또는 기존 저장소의 생성 이력 규칙을 적용한다. 중복 프레임 해시, 투명 모서리, 실제 화면 캡처를 검사한다.

### 2026-07-12 실제 제작 현황

- 완료: MVP 엔딩 일러스트 5/5, 엔딩 문양 5/5, 도감 썸네일 5/5, 진화 배지 6/6, 진화 초상 18/18, 전투 프레임 96/96, 진화 VFX 6/6세트, 스킬 아이콘 8/8, 유대·추억·기풍 UI 아이콘 3/3
- 게임 연결 완료: 엔딩 화면 배경·문양, 진화 선택 배지, 진화 완료 뒤 몬스터 화면 초상, 6개 진화 전투 애니메이션과 전용 스킬 효과, 관리 화면 스킬·유대·기억 아이콘
- 스킬 아이콘의 초기 계획 수량 `약 18개`는 추정치였으며, 실제 `data/skills.json` 등록 스킬 8개를 기준으로 8/8 완료했다.

위 그래픽 계약의 남은 수량은 0이다. 엔딩 도감 탐색 화면은 타이틀 메뉴에 구현했으며, 기억 상세 화면은 별도의 기능/UI 구현 범위다.

## 11. 권장 책임 분리

다음 이름은 방향을 설명하는 권장안이다. 실제 추가 시 기존 코드 스타일과 충돌 여부를 먼저 확인한다.

```text
DataRegistry
  정적 정의 로딩과 검증

MonsterRosterService
  개별 몬스터, 배치, 레벨, 유대, 진화, 스킬, 기록

RunMetricsTracker
  전투·관리·원정·관계 지표 누적

EndingResolver
  엔딩 후보, 점수, 동률, 최종 결과

ProfileState / NewCycleService
  영구 해금과 다음 회차 생성

CampaignSaveFacade
  기존 저장 안전 교체 로직과 v2 직렬화 연결
```

`GameRoot.gd`는 화면 전환과 서비스 연결을 맡고, 신규 계산 규칙의 실제 소유자가 되지 않는다.

## 12. Phase 작업 순서

각 Phase는 자동 검사와 완료 조건을 통과해야 다음으로 넘어간다. 여러 Phase를 한 변경 묶음으로 합치지 않는다.

### Phase 0 — 기준선 동결과 문서 등록

목표:

- 이 문서를 `docs/design`에 등록한다.
- 현재 브랜치와 커밋을 기록한다.
- 전체 자동 검증을 실행한다.
- 기준 화면 캡처를 남긴다.

완료 조건:

- `RunCoreVerification.ps1 -Mode Full` 전체 통과
- 기준 캡처 산출물 생성 시각이 이번 실행 이후
- `git diff --check` 통과
- 코드, 데이터, 그래픽 동작 변경 없음

금지:

- 저장 v2 구현
- 신규 JSON 추가
- 신규 캐릭터/스킬/그래픽 제작
- `GameRoot.gd` 수정

### Phase 1 — 데이터 계약과 검증기

목표:

- 종족, 개체, 스킬, 진화, 엔딩 규칙의 JSON 구조를 고정한다.
- 허용 필드, 자료형, 참조 무결성을 검사한다.

자동 검사:

- 올바른 예제 통과
- 필수 ID 누락, 중복 ID, 잘못된 참조, 알 수 없는 연산자 실패
- 기존 데이터 로딩 회귀 없음

금지:

- 저장 마이그레이션
- UI와 그래픽

### Phase 2 — 저장 v2와 개별 몬스터 마이그레이션

목표:

- 기존 v1을 읽어 `profile`과 `active_run`이 있는 v2로 안전 변환한다.
- 종족 키를 고정 개체 ID로 바꾼다.

완료 조건:

- v1 원본과 백업 보존
- 성공, 실패, 손상, 임시 파일, 백업 복구 검사 통과
- v1의 레벨, EXP, 배치, 승급, 특화, 원정 편성 손실 없음
- 실패 시 v1을 계속 사용할 수 있음

### Phase 3 — 누적 전투·원정 지표

목표:

- 기존 이벤트 지점에서 엔딩용 기록만 수집한다.
- 전투 판정 자체는 바꾸지 않는다.

완료 조건:

- 같은 시나리오의 지표가 결정적으로 동일
- 저장/불러오기 전후 동일
- 중복 기록 방지

### Phase 4 — 엔딩 조건 평가기

목표:

- `all / any / not / 비교` 조건 평가
- 점수, 동률 규칙, fallback 구현

완료 조건:

- 규칙 단위 검사
- 알 수 없는 metric과 연산자 거부
- 기존 승리 저장은 기본 엔딩 유지

### Phase 5 — 엔딩 도감 최소 구조

목표:

- 본 엔딩, 잠긴 엔딩, 힌트, 획득 날짜/회차 표시
- 아직 새 엔딩 연출은 만들지 않는다.

### Phase 6 — 푸딩 유대 수직 구현

목표:

- 출전 → 활약 → 결산 → 유대 상승 → 기억 해금의 한 줄 흐름을 푸딩에 완성한다.

완료 조건:

- 반복 호출로 유대가 중복 상승하지 않음
- 전투 불능/벤치로 감소하지 않음
- 저장/불러오기 유지

### Phase 7 — 푸딩 신규 스킬 실행기

목표:

- 필요한 최소 효과 타입과 푸딩 신규 스킬을 전투에 연결한다.

완료 조건:

- 기존 기술 회귀 없음
- 데이터가 잘못되면 안전하게 검증 실패
- 전투 성능 기준 통과

### Phase 8 — 푸딩 두 갈래 진화와 완전 그래픽

목표:

- 성문 방벽/구조 연금젤의 역할, AI, 스킬, 초상, 16프레임, VFX, UI 완성

진입 금지 조건:

- 임시 이미지 상태의 두 역할이 실제 전투에서 구분되지 않음
- 한 가지가 모든 상황에서 상위 호환

### Phase 9 — 곱 확장

푸딩에서 검증한 데이터와 실행기를 재사용해 매복대장/금고지기 두 경로를 완성한다. 곱만을 위한 별도 범용 시스템을 새로 만들지 않는다.

### Phase 10 — 핀 확장

화염 숙련자/잿불 주술사 두 경로를 완성한다. 기존 화염구와 승급 강화의 회귀를 특별히 검사한다.

### Phase 11 — 전체 몬스터 추억

핵심 몬스터의 개인 사건, 추억, 활약 문장과 후일담 연결을 확장한다. 동일한 사건을 반복 파밍하는 구조는 금지한다.

### Phase 12 — 특별 엔딩 4개

식구, 철벽, 위압, 라이벌 협정 엔딩을 실제 데이터와 화면에 연결한다. 기본 엔딩과 패배 재도전은 보존한다.

### Phase 13 — 새 회차

DAY 30 결산 → 엔딩 → 해금 → 계승 선택 → 다음 회차 시작을 연결한다.

완료 조건:

- 유지/초기화 목록이 계약과 일치
- 계승 한도 우회 불가
- 1회차 새 게임과 v1 이어하기 회귀 없음

### Phase 14 — 로로 성장

로로의 두 진화와 관계 역할을 앞 단계 시스템 위에 완성한다. 기존 원정 지원과 방어 참여 상태가 충돌하지 않게 한다.

### Phase 15 — 2차 엔딩과 신규 몬스터 1종

니아·골딘 계략 또는 바티 비밀 엔딩 중 검증 가치가 높은 하나를 먼저 만든다. 신규 몬스터도 한 종만 추가해 복수 개체와 모집 확장을 검증한다.

### Phase 16 — 통합 검수

다음 조합을 모두 검증한다.

- 신규 시작
- 기존 v1 이어하기
- v2 이어하기
- DAY 30 기본 엔딩
- 특별 엔딩 4개
- 새 회차 시작
- 압축 시작
- 세 핵심 몬스터의 양쪽 진화
- 로로 원정/방어 상태
- 저장 손상과 복구
- 1920×1080 및 지원 해상도 UI
- Web 빌드에서 파일 쓰기 제한

## 13. 공통 완료 기준

모든 Phase는 다음을 지킨다.

1. 수정 전 실패하는 검사를 먼저 정의한다.
2. 해당 Phase에 필요한 최소 파일만 수정한다.
3. 관련 전용 검사와 Quick 검증을 통과한다.
4. 저장/불러오기 영향이 있으면 손상·복구도 함께 검사한다.
5. 화면이 바뀌면 실제 캡처를 눈으로 확인한다.
6. Phase 완료 문서에 변경 파일, 명령, 결과, 남은 위험을 기록한다.
7. 다음 Phase 기능을 미리 넣지 않는다.

## 14. 2026-07-12 실행 결정

공유 문서의 최초 전달 지시는 Phase 0만 수행하는 것이었으나, 같은 날 이미 완료된 Full 13/13 검증을 다시 실행하는 중복이 발생했다. 사용자 지시에 따라 재검증을 중단하고, 기존 Full 13/13 결과를 기준선으로 재사용해 실제 기반 구현을 진행했다.

이번 실행에서 구현한 범위:

- Phase 0: 이 실행 기준 문서 등록
- Phase 1 기반: 개체 몬스터, 회차 지표, 엔딩 규칙 데이터와 검증기
- Phase 2 기반: v1 핵심 몬스터를 개체 ID로 옮기는 v2 변환기와 안전 저장소
- Phase 3 기반: 자료형·최솟값·최댓값을 지키는 회차 지표 수집기
- Phase 4 기반: `all / any / not / 비교` 조건식과 점수·우선순위·fallback 엔딩 평가기

초기 골격 작성 뒤 연결된 범위:

- 기존 `GameRoot.gd`의 실제 플레이 사건 → 회차 지표 기록
- DAY 30 새 평가기 → 엔딩 화면 선택
- 최소 엔딩 도감 수량 표시와 새 회차 시작
- 유대·기억·진화 분기의 플레이 화면 연결
- 엔딩 일러스트 5장, 진화 배지 6개, 진화 기본 초상 6개

2026-07-12 최종 완료 범위:

- 별도 엔딩 도감 상세 화면
- 기억별 회상 대사/사건과 몬스터별 기억 상세 화면
- 진화 전투 프레임·VFX·스킬 아이콘·감정 변형 초상
- 2회차 왕국 교리 대응·본 대화 건너뛰기·전투 x2 배속
- Python 직접 제작 런타임 그래픽의 GPT 내장 ImageGen 원본 교체

위 항목을 모두 구현하고 데이터·화면·저장 흐름에 연결했다.
