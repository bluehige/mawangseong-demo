# V1.2.2 릴리스 폴리시 F0-V 시각 인벤토리 핸드오프

## 1. 메타데이터

- 작성일: 2026-08-02
- 목표 버전: V1.2.2 release polish
- 작업 브랜치: `codex/v122-ui-simplification`
- 기준 브랜치와 SHA: `origin/main` merge-base `7ee0b50965dd3944a7ab737c0eca76d2df2a82ad`
- 마지막 기준 커밋 SHA: `efab13e7f9a2c1e2bd059eb8abf4cc409710380d`
- PR/태그: 없음 (사용자 요청 전 커밋·푸시하지 않음)

## 2. 이번 세션 목표

- 요청 사항: 루나 실행 계획의 첫 패킷 `F0-V`를 순서대로 실행한다.
- 완료 조건: 전투 활성 유닛 44개의 실제 스프라이트 경로, 원본/프레임 크기, 투명 영역, 발 위치 추정, 데이터·런타임 비행 판정, `PASS / NEEDS_NORMALIZATION / DISCONNECTED`를 표로 고정하고 DAY 3 대표 1280×720 화면을 확보한다.
- 제외 범위: 런타임 코드·전투 수치·그래픽 원본·import 설정 수정, 자산 삭제/재생성, 전체 회귀, 전체 플레이, 빌드 및 커밋.

## 3. 완료 내용

- 6개 전투 데이터 소스에서 44개 활성 레코드를 모아 읽기 전용 이미지 인벤토리를 생성했다.
- 192×192 28개, 1254×1254 14개, 768×768 2개의 혼합 프레임 기준을 확인했다.
- 1254 시트의 4×4 셀(313.5px), 기본 코볼트·문 트래커의 1254 단일 프레임, 불투명 크로마키 원본, 5개 중복 스프라이트 경로를 확인했다.
- `imp`와 `dusk_courier`의 데이터/런타임 비행 판정 불일치를 기록했다. `bat_courier`는 F0에서 이름만으로 비행을 확정하지 않았다.
- 활성 로스터 연락판과 DAY 3 대표 1280×720 전투 캡처를 `tmp/v122_release_polish/f0_v/`에 생성했다. 이 폴더는 임시 검수 산출물이며 소스 브랜치에 커밋하지 않는다.

## 4. 변경 파일

| 경로 | 변경 목적 | 상태 |
|---|---|---|
| `docs/qa/V122_F0V_VISUAL_INVENTORY_2026-08-02.md` | F0-V 판정표·구조적 원인·다음 순서 기록 | 완료 |
| `docs/handoff/V122_RELEASE_POLISH_F0V_VISUAL_INVENTORY_2026-08-02.md` | 이번 세션 인계 문서 | 완료 |
| `docs/handoff/CURRENT.md` | 다음 작업을 F0-A로 연결 | 기존 계획 문서에서 이미 수정됨 |

런타임·데이터·그래픽 자산은 수정하지 않았다. 기존 작업 트리에 있던 6개 `.import` 변경과 7개 `.uid` 파일은 건드리지 않았다.

## 5. 그래픽 자산과 실제 렌더 확인

- GPT 내부 이미지 생성 모델 사용: 해당 없음 (읽기 전용 감사).
- 생성 원본/런타임 자산 변경: 없음.
- 대표 화면 확인: DAY 3 전투 장면을 1280×720으로 비헤드리스 실행해 저장했다. 현재 화면의 유닛·벽·UI가 함께 보이며 자산 정규화 전의 기준선으로 사용한다.

## 6. 테스트 및 검수

| 순서 | 검증 방법 | 결과 | 근거 |
|---:|---|---|---|
| 1 | Python/Pillow 읽기 전용 인벤토리 생성(44개 레코드, 파일 존재·크기·alpha bbox·중복·비행 판정) | PASS | `tmp/v122_release_polish/f0_v/f0_v_inventory.json` |
| 2 | `godot --path . --scene res://tmp/v122_release_polish/f0_v/F0VDay3Capture.tscn` | PASS | `F0V_DAY3_CAPTURE ... size=(1280, 720)` 출력 및 PNG |
| 3 | 전체 회귀/전체 플레이/별도 검수 에이전트 | NOT_REQUESTED | 이번 요청 범위가 F0-V 읽기 전용 인벤토리임 |

### 필수 자동 검수 고정 필드

- Related tests: `F0-V Python/Pillow inventory`, `F0-V DAY3 capture` — 모두 PASS.
- UI check: `1280×720 DAY 3` 대표 화면 저장 PASS.
- Unresolved issues: 25개 `NEEDS_NORMALIZATION`; 1254 크로마키/프레임 기준, 중복 경로, 비행 태그 불일치는 F1 이후 정규화해야 한다.

## 7. 미해결 문제와 위험

- 1254 원본을 단순히 삭제하거나 재생성하면 캐릭터 정체성과 애니메이션 셀 대응을 잃을 수 있으므로 F0에서 처리하지 않았다.
- `dusk_courier`의 비행 프로필을 코드에 바로 반영하지 않았다. F0-A/F0-R에서 데이터 계약을 먼저 정한 뒤 런타임 수정한다.
- `bat_courier`의 비행 여부는 기획 태그가 없으므로 다음 패킷에서 확정해야 한다.

## 8. 다음 작업 순서

1. `F0-A`: BGM·타격음·피격음·UI/환경음의 실제 연결과 누락을 읽기 전용으로 인벤토리한다.
2. `F0-R`: 바닥 접지·그림자·맵 상대 높이의 공통 렌더 계약을 고정한다.
3. `V1-C`: 승인된 1254 시트만 192 셀로 전처리하고 출처 문서를 연결한다.
4. `V1-R`: 유닛별 렌더 프로필과 비행/지상 앵커를 코드에 적용한다.

## 9. 작업 트리 상태

- 브랜치: `codex/v122-ui-simplification` (origin보다 2커밋 앞섬)
- 이번 F0-V 문서 2개와 기존 계획 문서는 아직 미커밋이다.
- 기존 사용자 변경: 6개 `assets/sprites/enemies/update4/region/*.png.import` 수정, 7개 테스트/도구 `.uid` 미추적 파일. 보존만 하고 스테이징하지 않았다.
- 임시 캡처·분석 파일: `tmp/v122_release_polish/f0_v/`; 소스 브랜치 커밋 대상이 아니다.
- 커밋/푸시: 수행하지 않음.

## 10. 종료 체크리스트

- [x] F0-V 인벤토리와 판정표 작성
- [x] DAY 3 대표 1280×720 캡처
- [x] `CURRENT.md`에 다음 패킷 연결
- [x] 관련 범위의 최소 검증 실행
- [ ] F0-A 시작
- [ ] 커밋/푸시 (사용자 요청 전 보류)
