# 마왕성 데모 완료 상태

이 파일은 현재 데모가 어디까지 완성됐는지, 그리고 검수 후 무엇을 손보면 되는지 빠르게 확인하기 위해 필요하다.

작성일: 2026-07-01

## 1차 완료 결론

필수 플레이 루프와 1차 구조 정리는 완료 상태다.

자동 스모크 테스트 기준으로 다음 흐름이 모두 통과했다. 스모크 테스트는 큰 기능이 최소한 끊기지 않는지 빠르게 확인하는 자동 점검이다.

```powershell
godot --headless --path . --scene res://tools/DemoSmokeTest.tscn
```

결과:

```text
DEMO_SMOKE_TEST: PASS
```

실제 렌더링 화면 캡처도 저장했다. 캡처는 관리, 몬스터, 전투 시작, 전투 조작, 결과 화면을 실제 창 렌더링으로 찍어 UI 겹침과 텍스트 넘침을 확인하기 위한 것이다.

```powershell
godot --path . --scene res://tools/ManualVerificationCapture.tscn
```

캡처 위치:

```text
tmp/manual_verification/
```

## 필수 체크리스트

- [x] 프로젝트 실행 시 관리 화면이 뜬다.
- [x] 방어 준비 후 전투 화면으로 이동한다.
- [x] 적이 입구에서 등장해 목표 방 또는 교전 목표를 잡는다.
- [x] 슬라임, 고블린, 임프가 자동으로 적을 공격한다.
- [x] 임프가 화염구 투사체를 발사한다.
- [x] 가시 복도 함정이 적에게 피해를 준다.
- [x] 전체 지침 변경이 전투 행동에 반영된다.
- [x] 방 지침 변경이 특정 방 행동에 반영된다.
- [x] 몬스터 하나를 직접 조종할 수 있다.
- [x] 왕좌의 방 HP가 0이면 패배한다.
- [x] 3일차 수련생 용사를 잡으면 데모 클리어가 뜬다.
- [x] 결산 화면이 표시된다.

## 추가 완료 항목

- [x] 방어 준비 -> 전투 -> 결과 화면까지 자동 플레이 검증.
- [x] 직접 조종 우클릭 이동 검증.
- [x] 키보드 1번/2번 스킬 입력 검증.
- [x] 임프 화염구 스킬 입력과 투사체 표시 검증.
- [x] UI 겹침/텍스트 넘침 방지를 위해 HUD 글자 크기, 말줄임, 패널 간격 조정.
- [x] 실제 렌더링 캡처로 관리/전투/결과 화면 확인.
- [x] `GameRoot.gd` 책임을 `ManagementSceneController`, `CombatSceneController`, `HUDController`로 분리.
- [x] 단일 이미지 캐릭터를 `AnimatedSprite2D` + `SpriteFrames` 구조로 확장.

## 던전 맵 보강 완료 항목

2026-07-01 추가 수정 기준이다. 기존 화면은 방 사각형을 UI처럼 얹은 상태에 가까웠기 때문에, 참고 이미지의 탑뷰 타일 던전 느낌을 기준으로 먼저 보강했다.

- [x] `topview_battle_ui_reference.png`, `castle_management_ui_reference.png`, `concept_asset_sheet.png`를 확인하고 방향을 맞췄다.
- [x] 방 좌표를 64px 타일 배수 기준으로 다시 배치했다.
- [x] 입구 -> 가시 복도 -> 중앙 통로 -> 왕좌의 방으로 이어지는 주 방어 동선을 만들었다.
- [x] 병영, 회복 둥지, 건설 슬롯, 보물 보관실이 중앙 통로에서 갈라지는 던전 구조로 보이게 조정했다.
- [x] `DungeonRenderer.gd`를 추가해 배경, 복도, 방, 암벽 테두리, 바닥 타일, 방 소품, 방 이름 표지를 별도 렌더링 책임으로 분리했다.
- [x] 관리 화면에서도 몬스터가 배치된 방 안에 대기하는 모습이 보이도록 했다.
- [x] 새 맵 좌표에 맞춰 적 스폰 위치와 몬스터 대기 위치를 보정했다.
- [x] 방 이름 표지가 캐릭터를 가리지 않도록 위치를 조정했다.
- [x] 전투 로그 패널 폭을 줄여 입구 쪽 캐릭터 침범을 줄였다.
- [x] 방마다 따로 벽을 두르던 표현을 제거하고, 이동 가능한 바닥 타일을 먼저 만든 뒤 바깥쪽에 벽 타일을 붙이는 방식으로 바꿨다.
- [x] 입구 -> 가시 복도 -> 중앙 통로 -> 왕좌의 방 주 경로와, 병영/회복 둥지/보물 보관실 분기 경로가 한눈에 보이도록 수정했다.

검증:

```powershell
godot --headless --path . --scene res://tools/DemoSmokeTest.tscn
godot --path . --scene res://tools/ManualVerificationCapture.tscn
```

결과:

```text
DEMO_SMOKE_TEST: PASS
tmp/manual_verification/01_management.png
tmp/manual_verification/03_combat_start.png
tmp/manual_verification/04_combat_controls.png
```

## 검수 후 할 일

아래 항목은 1차 완성 뒤 실제 플레이 감각을 보며 손보면 좋다.

- 실제 사람 플레이 기준 전투 템포와 난이도 체감 확인
- 던전 벽 코너, 문턱, 계단, 방별 장식 밀도를 레퍼런스 수준에 가깝게 추가 보강
- 1~3일차 보상량, 적 체력, 왕좌 HP 피해량 밸런스 조정
- 애니메이션 프레임 추가 제작 및 SpriteFrames에 실제 프레임 삽입
- 미니맵, 사운드 이펙트, 튜토리얼 대사 추가
- 레벨업 스킬 후보 선택 UI 추가
- 자유 건설 시스템 확장

## 현재 기준

이번 완료 기준은 "상용 출시 수준"이 아니라 "참고 이미지의 방향성을 반영한 타일형 던전 위에서, 처음부터 3일차 클리어 또는 패배까지 연결되는 플레이 가능한 데모"다.
