# v1.2.2 V2-P4C `war_drummer` 기본 전투 자산

## 패킷 카드

- `PACKET_ID`: `V2-P4C-ASSET-WAR`
- `GOAL`: 두둠(`war_drummer`) 기본 계약 전투 자산 1개만 생성하고 출처·런타임 시트를 기록
- `ALLOWED_WRITE_PATHS`: 두둠 원본 1개, `SOURCE.md` 1개, 런타임 시트 1개, 이 QA, 핸드오프, `CURRENT.md`
- `FORBIDDEN`: 제품 데이터·코드 연결, 다른 캐릭터, 초상화·VFX·오디오, CONNECT 패킷, 공통 V3 정규화
- `DIRECT_TEST`: 4×4/16셀/행 계약, 투명도·크로마 잔류·파일 해시·공통 바닥 기준선 검사
- `UI_OR_AUDIO_CHECK`: 없음 — 자산 생성 패킷
- `STOP_AFTER`: 자산과 직접 검수 기록 후 중단

## 결과

`ASSET_PASS_WITH_CONNECT_PENDING`.

두둠 전용 기본 계약 전투 원본과 192×192 셀 16프레임 런타임 시트를 만들었다. 해골·북·북채·깃털이 있는 작은 전투 북잡이로 분리했으며, 첫 생성에서 잘못 나온 `down` 행을 폐기하고 행별 동작 계약을 명시한 두 번째 생성본을 최종 채택했다. 초상화·VFX·오디오·제품 데이터 연결은 실행하지 않았다.

## 변경 자산

| 역할 | 경로 | 형식/크기 | SHA-256 앞 16자 |
|---|---|---:|---|
| 생성 원본 | `assets/source/imagegen/update4_contract_monsters/dudum/dudum_combat_sheet_chroma_2026-08-04.png` | RGB 1254×1254 / 1,861,921 bytes | `00e1486edcdd67d4` |
| 런타임 시트 | `assets/sprites/monsters/update4/monster_dudum_sheet.png` | RGBA 768×768, 셀 192×192 / 471,379 bytes | `50dd94d1594b62ae` |
| 출처 기록 | `assets/source/imagegen/update4_contract_monsters/dudum/SOURCE.md` | 생성·후처리·검수 메타데이터 | — |

## 직접 검사

- 원본 크기 `1254×1254 RGB`, 런타임 크기 `768×768 RGBA`: PASS
- 런타임 4열×4행의 16셀 모두 visible pixel 포함: PASS
- 16개 셀 프레임 해시 모두 다름: `16/16` unique, PASS
- 네 모서리 알파 `0`, 모든 셀 외곽 투명: PASS
- 행 계약 시각 확인: 1행 `idle 2 + 옆으로 쓰러진 down 2`, 2행 걷기, 3행 북 공격, 4행 황동 리듬 지원: PASS
- 셀별 그림 높이 중앙값: `140.0px`
- 공통 바닥 기준선: 모든 셀 `bottom=183`, spread `0px`
- 순수 크로마 잔류 검사(`alpha >= 24`, `green >= 100`, `red <= 25`, `blue <= 25`): `0개`
- 녹색 우세 잔류 검사(`alpha >= 24`, `green - red >= 60`, `green - blue >= 60`): `0개`
- 크로마 제거·프레임 배정 중 큰 애매 성분: `0개`; 작은 장식 성분 1개는 원래 프레임에 보존됐고 런타임 외곽 침범 없음
- 생성 원본과 투명 런타임 시트를 실제 이미지로 확인: 녹색 배경 제거, 두둠 실루엣·북·행별 동작 확인 PASS
- 제품 데이터와 `combat_visual_profiles.json` 연결은 다음 CONNECT 패킷으로 분리했다.

## 다음 패킷 제안

- `NEXT_PACKET_ID_PROPOSAL`: `V2-P4C-CONNECT-WAR`
- 목표: 두둠 런타임 시트 1개만 `data/monsters.json`과 `data/v122/combat_visual_profiles.json`에 연결하고 직접 계약 검사
- 실행하지 않은 항목: 제품 연결, 렌더 배율·접지 앵커 계산, `1280×720` 대표 화면 확인
- 이 제안은 다음 turn의 카드일 뿐이며, 현재 turn에서는 실행하지 않는다.

## 검수 상태

- `Related tests`: 4×4/16셀/행 계약, RGBA 투명도, 순수 크로마 0, 녹색 우세 0, 공통 바닥선, 중복 해시 0, SHA-256 검사 PASS
- `UI check`: CONNECT 패킷 전이라 실행하지 않음
- `Unresolved issues`: 제품 데이터와 visual profile이 아직 고블린 공유 경로를 참조하므로 `V2-P4C-CONNECT-WAR` 전에는 게임에서 두둠 전용 그림이 표시되지 않음
